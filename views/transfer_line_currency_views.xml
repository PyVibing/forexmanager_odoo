<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    
    <!-- List view of forexmanager.transfer.line -->
    <record id="forexmanager_transfer_line_currency_list_view" model="ir.ui.view">
        <field name="name">ForexManager transfer line currency list_ view</field>
        <field name="model">forexmanager.transfer.line.currency</field>
        <field name="arch" type="xml">
            <list>
                <field name="currency_id"/>
                <field name="amount"/>
            </list>
        </field>
    </record>
</odoo>
